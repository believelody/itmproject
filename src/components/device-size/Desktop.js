import React, { Fragment } from 'react';
import { Main, Footer, Header } from '../Export';

const Desktop = () => (
  <Fragment>
    <Header />
    <Main />
    <Footer />
  </Fragment>
);

export default Desktop;
