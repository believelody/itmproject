import React from 'react';
import { Alert } from 'reactstrap';

const Footer = ({}) => (
  <footer>
    <Alert color='dark'>
      &copy; Copyright
    </Alert>
  </footer>
);

export default Footer;
